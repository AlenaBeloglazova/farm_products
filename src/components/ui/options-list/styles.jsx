import styled from "styled-components";

export const Option = styled.li`
  margin: 0;
  margin-bottom: 8px;
  font-size: 14px;
  list-style: none;
  padding: 0;

  :last-child {
    margin-bottom: 0;
  }
`;

export const Property = styled.span`
  font-weight: bold;
`;
